import LandingPage from "@/src/components/home/LandingPage";

// The landing page stays server-rendered because it only presents static content.
export default function Home() {
  return <LandingPage />;
}
